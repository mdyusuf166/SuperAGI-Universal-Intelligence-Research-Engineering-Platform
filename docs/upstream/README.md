# Upstream research projects

This directory records provenance, licensing, versions, compatibility notes,
and integration boundaries for upstream projects studied by SuperAGI.

Upstream code is kept under `third_party/` and is not part of the SuperAGI
runtime package. Each project document is updated from checked-out repository
metadata after cloning.
